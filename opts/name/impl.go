package optvar

import (
	// "fmt"
	// "github.com/docker/docker/api/types"
)

func init() {
	// mod.impl = func(container types.ContainerJSON) string {
	// 	return fmt.Sprintf("--name %v", container.Name)
	// }
}
